# FROM halvves/mongodb-backup-s3:lates

# RUN apt-get update && apt-get -y install cron && apt-get -y install awscli

# RUN (crontab -l ; echo "* * * * * /dump.sh >> /var/log/cron.log") | crontab

# CMD cron